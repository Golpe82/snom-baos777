<?xml version="1.0"?>

<SnomIPPhoneMenu document_id='control'>
<Title>KNX Snom</Title>
{% if subgroups %}
    {% for subgroup in subgroups %}
        <MenuItem>
        <Name>{{subgroup}}</Name>
            <URL>{{knx_gateway}}minibrowser/{{maingroup}}/{{subgroup}}/</URL>
        </MenuItem>
    {% endfor %}
{% else %}
    <MenuItem>
        <Name>no knx subgroupaddresses</Name>
    </MenuItem>
    <fetch mil='1500'>snom://mb_exit</fetch>
{% endif %}
    <fetch mil='100000'>snom://mb_exit</fetch>
</SnomIPPhoneMenu>
